#include "print.h"

int main() {
	print();
	return 0;
}

/* 1b. O endereço vec guarda a posição de memória onde vec se encontra, ou seja, guarda o endereço do primeiro valor do vetor vec.
 * Por sua vez, o endereço de vec[0] é o mesmo que o endereço de vec. Já os endereços de vec[1] e vec[2] têm o valor do endereço de
 * vec mais 4 e o valor do endereço de vec[1] mais 4, respetivamente , uma vez que vec é um vetor que guarda valores do tipo int, que ocupam 4 bytes na memoria.
 */
 
 /*1c. Se o programa fosse corrido em computadores diferentes os endereços das variaveis seriam diferentes */

